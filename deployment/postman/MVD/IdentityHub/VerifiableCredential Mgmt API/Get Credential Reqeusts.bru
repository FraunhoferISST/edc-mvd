meta {
  name: Get Credential Reqeusts
  type: http
  seq: 5
}

get {
  url: {{CS_URL}}/api/identity/v1alpha/participants/{{PARTICIPANT_ID_BASE64}}/credentials/request/{{REQUEST_ID}}
  body: json
  auth: inherit
}

settings {
  encodeUrl: true
}
