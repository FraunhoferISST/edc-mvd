meta {
  name: query Credentials
  type: http
  seq: 1
}

post {
  url: {{ISSUER_ADMIN_URL}}/api/admin/v1alpha/participants/{{ISSUER_CONTEXT_ID}}/credentials/query
  body: json
  auth: inherit
}

body:json {
  {
  }
}

settings {
  encodeUrl: true
}

docs {
  Query credentials, possibly across multiple participants.
}
