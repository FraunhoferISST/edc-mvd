meta {
  name: delete Credential Definition By Id
  type: http
  seq: 5
}

delete {
  url: {{ISSUER_ADMIN_URL}}/api/admin/v1alpha/participants/{{ISSUER_CONTEXT_ID}}/credentialdefinitions/:credentialDefinitionId
  body: none
  auth: inherit
}

params:path {
  credentialDefinitionId: <string>
}

headers {
  Accept: application/json
}

settings {
  encodeUrl: true
}

docs {
  Deletes a credential definition by its ID.
}
